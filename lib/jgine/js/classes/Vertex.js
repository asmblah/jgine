/*
 *  jGine Vertex class
 */
define([
    "js/util"
], function(
    util
) {

    function Vertex() {
        util.assertConstructor(this, Vertex);
    }

    util.extend(Vertex, {

    });

    util.extend(Vertex.prototype, {

    });

    // Export
    return Vertex;
});
