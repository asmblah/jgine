/*
 *  jGine Camera class
 */
define([
    "js/util"
], function(
    util
) {

    function Camera() {
        util.assertConstructor(this, Camera);
    }

    util.extend(Camera, {

    });

    util.extend(Camera.prototype, {

    });

    // Export
    return Camera;
});
