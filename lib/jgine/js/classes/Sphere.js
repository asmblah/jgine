/*
 *  jGine Sphere class
 */
define([
    "js/util"
], function(
    util
) { "use strict";

    function Sphere() {
        util.assertConstructor(this, Sphere);
    }

    util.extend(Sphere, {

    });

    util.extend(Sphere.prototype, {

    });

    // Export
    return Sphere;
});
