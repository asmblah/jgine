/*
 *  jGine Sphere class
 */
define([
    "js/util"
], function(
    util
) {

    function Sphere() {
        util.assertConstructor(this, Sphere);
    }

    util.extend(Sphere, {

    });

    util.extend(Sphere.prototype, {

    });

    // Export
    return Sphere;
});
