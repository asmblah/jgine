/*
 *  jGine Box (Cuboid) class
 */
define([
    "js/util"
], function(
    util
) {

    function Box() {
        util.assertConstructor(this, Box);
    }

    util.extend(Box.prototype, {

    });

    // Export
    return Box;
});
