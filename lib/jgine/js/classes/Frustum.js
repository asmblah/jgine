/*
 *  jGine Frustum class
 */
define([
    "js/util"
], function(
    util
) {

    function Frustum() {
        util.assertConstructor(this, Frustum);
    }

    util.extend(Frustum, {

    });

    util.extend(Frustum.prototype, {

    });

    // Export
    return Frustum;
});
