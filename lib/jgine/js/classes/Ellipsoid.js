/*
 *  jGine Ellipsoid class
 */
define([
    "js/util"
], function(
    util
) { "use strict";

    function Ellipsoid() {
        util.assertConstructor(this, Ellipsoid);
    }

    util.extend(Ellipsoid, {

    });

    util.extend(Ellipsoid.prototype, {

    });

    // Export
    return Ellipsoid;
});
