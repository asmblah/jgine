/*
 *  jGine Texture class
 */
define([
    "js/util"
], function(
    util
) {

    function Texture() {
        util.assertConstructor(this, Texture);
    }

    util.extend(Texture, {

    });

    util.extend(Texture.prototype, {

    });

    // Export
    return Texture;
});
