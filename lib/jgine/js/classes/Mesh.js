/*
 *  jGine Mesh class
 */
define([
    "js/util"
], function(
    util
) {

    function Mesh() {
        util.assertConstructor(this, Mesh);
    }

    util.extend(Mesh, {

    });

    util.extend(Mesh.prototype, {

    });

    // Export
    return Mesh;
});
