/*
 *  jGine Octree class
 */
define([
    "js/util"
], function(
    util
) { "use strict";

    function Octree() {
        util.assertConstructor(this, Octree);
    }

    util.extend(Octree.prototype, {

    });

    // Export
    return Octree;
});
