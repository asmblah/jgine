/*
 *  jGine Ray (Infinite Vector) class
 */
define([
    "js/util"
], function(
    util
) { "use strict";

    function Ray() {
        util.assertConstructor(this, Ray);
    }

    util.extend(Ray, {

    });

    util.extend(Ray.prototype, {

    });

    // Export
    return Ray;
});
