/*
 *  jGine Edge class
 */
define([
    "js/util"
], function(
    util
) { "use strict";

    function Edge() {
        util.assertConstructor(this, Edge);
    }

    util.extend(Edge, {

    });

    util.extend(Edge.prototype, {

    });

    // Export
    return Edge;
});
