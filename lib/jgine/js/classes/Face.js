/*
 *  jGine Face class
 */
define([
    "js/util"
], function(
    util
) {

    function Face() {
        util.assertConstructor(this, Face);
    }

    util.extend(Face, {

    });

    util.extend(Face.prototype, {

    });

    // Export
    return Face;
});
